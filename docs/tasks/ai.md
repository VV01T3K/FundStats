apps/website/src/routes/api/ai/chat.ts

move the whole integration to the integration folder , also the api endpoint and make use of durable streams as the transport layer so we dont have to do that ourselves and for future integrations
